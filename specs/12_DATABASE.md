# 12 — Database

Tables: workspaces, evidence_items, file_metadata, review_answers, connections, usefulness_scores, review_history, exports, export_items, binder_generations, scan_runs, duplicates, settings.

Requirements: foreign keys, migrations, stable IDs, autosave, audit status changes, no original bytes in DB, relative paths where practical.
