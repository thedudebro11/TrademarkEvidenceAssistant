{
  "name": "@trademark-evidence-assistant/shared",
  "private": true,
  "version": "0.1.0",
  "description": "Shared TypeScript models, DTOs, enums, and validation schemas",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
